# AutoX.js v7 二次开发指南

> 项目版本：7.2.3（`project-versions.json`，versionCode 723）
> 分支：`setup-v7` ｜ 协议：GPL-V2（源自 Auto.js 的 MPL-2.0 + 非商业性使用）
> 官方文档：https://autox-doc.vercel.app/ ｜ 更新日志：`CHANGELOG.md`

## 1. 模块地图

| 模块 | 角色 | 二次开发常见入口 |
|---|---|---|
| `app` | 主应用壳与全部 UI（Compose + ViewBinding） | `app/src/main/java/org/autojs/autojs/ui/`：explorer（文件管理）、edit（编辑器）、settings、floating（悬浮窗/布局分析） |
| `autojs` | 核心引擎：脚本运行时、Rhino 集成、JS API、无障碍服务 | `autojs/src/main/java/com/stardust/autojs/`：`runtime/`（ScriptRuntime、ScriptBridges）、`core/`（accessibility、floaty、http、image、ui 等 30+ 能力包）、`engine/` |
| `automator` | 自动化操作层（无障碍点击/查找/通知） | `automator/src/main/java/com/stardust/automator/`：`search/`（控件查找）、`simple_action/`（动作执行） |
| `common` | 公共基础库（工具、图像、网络等） | `common/src/main/java/com/stardust/` |
| `inrt` | Rhino JavaScript 引擎封装 | `inrt/src/main/` |
| `apkbuilder` | 将脚本打包成独立 APK 的组件 | `apkbuilder/src/main/java/` |
| `paddleocr` | OCR 识别（可选组件） | `paddleocr/src/main/` |
| `codeeditor` | 代码编辑器组件 | `codeeditor/src/main/java/` |
| `docs/v2` | 文档子模块（git@ SSH，未初始化，不影响构建） | — |

**JS 侧内置资源**：`autojs/src/main/assets/`（`init.js`、`modules/`）——新增内置 JS API 时同时改这里和 `runtime/api` 下的 Java 绑定。

## 2. 构建环境（本机已就绪）

| 组件 | 要求 | 本机状态 |
|---|---|---|
| JDK | 17 | ✅ Temurin 17.0.20.1（`/Users/apple/.workbuddy/binaries/java/jdk-17.0.20/Contents/Home`） |
| Android SDK | compileSdk 34 / target 34 / minSdk 27，build-tools 34.0.0 | ✅ 已补装 platform-34 + build-tools 34.0.0 |
| Gradle | 8.7（wrapper 自动下载，已缓存） | ✅ |
| 网络 | 构建源含阿里云镜像；GitHub/Google 需代理 `http://127.0.0.1:7897` | ✅ `local.properties` 已指向 SDK |

## 3. 常用构建命令

```bash
cd /Users/apple/DoubaoWork/chats/2026-09-10/new-chat-3/AutoX

# 联网/下载时挂代理（本机默认需要）
export JAVA_HOME=/Users/apple/.workbuddy/binaries/java/jdk-17.0.20/Contents/Home
export HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 ALL_PROXY=http://127.0.0.1:7897

# 渠道 flavor：common（正式包）、v7（Autox.js v7 品牌包）、v7_mini（精简包）、suncV7（自建渠道）
./gradlew :app:assembleCommonDebug    # debug APK
./gradlew :app:assembleV7Debug
./gradlew :app:assembleSuncV7Debug    # 自建渠道
./gradlew :app:assembleCommonRelease  # release（未配签名时产出未签名包）
./gradlew :app:installCommonDebug     # 直接安装到已连接设备
```

产物位置：`app/build/outputs/apk/<flavor>/<type>/app-<flavor>-arm64-v8a-<type>.apk`
（`splits` 仅打 arm64-v8a；如需全 ABI 可改 `app/build.gradle.kts` 的 `splits` 段）

### 自建渠道 suncV7

- 定义：`app/build.gradle.kts` 的 `productFlavors.create("suncV7")`
- 包名：`org.autojs.autoxjs.suncV7`（可与其他渠道并存安装）
- 显示名：suncV7（manifest 占位符 `${appName}`）
- 版本线：独立 versionCode 1000 / versionName 1.0.0（不随官方 723 走，自己控制）
- `BuildConfig.CHANNEL = "suncV7"`：代码里可用它做渠道分支
- 专属资源：`app/src/suncV7/res/`（当前含全套 mipmap 图标，改图标/文案只动这个目录）

## 4. 本机已产出

| APK | 路径 | 说明 |
|---|---|---|
| common debug | `app/build/outputs/apk/common/debug/app-common-arm64-v8a-debug.apk`（157M） | 正式包名 `org.autojs.autoxjs.common`，debug 签名可直接安装 |
| v7 debug | `app/build/outputs/apk/v7/debug/app-v7-arm64-v8a-debug.apk`（157M） | 品牌包名 `org.autojs.autoxjs.v7` |
| suncV7 debug | `app/build/outputs/apk/suncV7/debug/app-suncV7-arm64-v8a-debug.apk`（157M） | 自建渠道，包名 `org.autojs.autoxjs.suncV7`，版本 1.0.0 (1000)，显示名 suncV7 |

两个包可同时安装、互不冲突，适合对照开发。debug 包体积大属正常（未压缩 + 含全部 native 库与 OCR 模型资源）。

## 5. 常用二次开发切入点

1. **新增/修改 JS API**：`autojs/src/main/java/com/stardust/autojs/runtime/ScriptBridges.kt`（Java↔JS 桥），或 `core/api` 下对应能力包；内置 JS 文件在 `autojs/src/main/assets/modules/`。
2. **自动化能力（无障碍）**：`automator/src/main/java/com/stardust/automator/`——控件查找在 `search/`，执行动作在 `simple_action/`。
3. **UI 与交互**：`app/src/main/java/org/autojs/autojs/ui/`，主界面为 Compose（`explorer/`），旧界面为 XML + ButterKnife；改资源在 `app/src/main/res/`。
4. **悬浮窗/布局分析**：`app/src/main/java/org/autojs/autojs/ui/floating/`。
5. **打包脚本为 APK**：`apkbuilder` 模块。
6. **版本号/渠道**：改 `project-versions.json`（版本号、SDK 版本）与 `app/build.gradle.kts`（flavor、applicationId）。

## 6. 发布构建（已配置好）

- 发布 keystore：`~/keystores/sunc-release.jks`（别名 `sunc`，密码 `666888`，有效期 27 年）
  ⚠️ **务必备份 keystore 与密码**——丢失后无法再更新已发布应用
- 签名配置：`~/.gradle/gradle.properties`（本地文件，不入库，权限 600）
- 构建已签名发布版：`./gradlew :app:assembleSuncV7Release`
- 产物：`app/build/outputs/apk/suncV7/release/app-suncV7-arm64-v8a-release.apk`（143M）
- 验证签名：`apksigner verify --print-certs <apk>`
- `app/build.gradle.kts` 签名优先级：本地 `SIGNING_*` 属性 → CI 环境变量（CI 逻辑保留）

## 7. 发布注意

- release 签名：CI 通过环境变量 `KEYSTORE_BASE64 / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD` 注入（见 `app/build.gradle.kts`）；本机构建 release 不会自动签名，需自行配置 keystore。
- 包名后缀：`common` 渠道 applicationId 带 `.common`，若需发布官方包名需在 flavor 中调整。
- 改动遵守 GPL-V2：本项目源于 Auto.js（MPL-2.0 + 非商业性），商用前请核对两份 LICENSE 约束。

## 7. IDE 使用

- 用 Android Studio 直接打开项目根目录（`new-chat-3/AutoX`），SDK 位置已在 `local.properties` 写好。
- Gradle JDK 选 17；首次 Sync 会走代理下载，可把 `~/.gradle/gradle.properties` 里加：
  ```
  systemProp.http.proxyHost=127.0.0.1
  systemProp.http.proxyPort=7897
  systemProp.https.proxyHost=127.0.0.1
  systemProp.https.proxyPort=7897
  ```
- 单元测试：`./gradlew :autojs:testDebugUnitTest`（autojs 模块自带 v6/v7 测试脚本）。
